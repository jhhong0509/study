spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      port: 27017
      host: localhost
      database: test
      username: root
      password: root
  redis:
    host: localhost
    port: 6379
auth:
  secret: localTestSecret
  exp:
    access: 10000
    refresh: 1000000